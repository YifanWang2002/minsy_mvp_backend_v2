# macd
category: momentum
summary: Moving Average Convergence Divergence for trend momentum and crossover confirmation.
params: fast(int), slow(int), signal(int), source(str, default=close)
outputs: macd_line, signal, histogram
